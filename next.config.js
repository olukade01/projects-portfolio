/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // basePath: "/public",
  images: {
    domains: ["images.unsplash.com"],
  },
};

module.exports = nextConfig;
